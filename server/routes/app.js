module.exports = function( app, server_detail, models ,console) {
  // console.log("models :: ", models);
  const path = require('path');

  // here you can write path to redirect to hit api functions
  
  // const campaignApi = require('./apis/campaigns.js')(app, server_detail , console);
  // app.get('/getGeofence',campaignApi.getGeofence);
  
};
